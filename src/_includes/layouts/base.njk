<!DOCTYPE html>
<html dir="rtl" lang="{{ meta.lang }}">
<head>
  <title>{{ meta.title }} | {{ title }}</title>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="{{ description }}">
  <link rel="stylesheet" href="{{ '/assets/style.css' | url }}" media="print" onload="this.media='all'">
  <link type="application/atom+xml" rel="alternate" href="{{ meta.feedUrl }}" title="{{ meta.title }}">
</head>

<body>
        {% include "header.njk" %}
    <main>
            {{ content | safe }}
    </main>
        {% include "footer.njk" %}
    <script>{% include "src/assets/js/search-new.js" %}</script>
</body>
</html>
